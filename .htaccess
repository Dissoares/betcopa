<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # encaminha todos os pedidos para a pasta public, exceto quando já estivermos lá
  RewriteCond %{REQUEST_URI} !^/public/
  RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
